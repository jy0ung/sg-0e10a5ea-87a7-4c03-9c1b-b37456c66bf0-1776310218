{
  "name": "@flc/shell",
  "version": "0.0.1",
  "private": true,
  "description": "Shared app-shell types and pure utilities consumed by both the main and HRMS web apps. JSX components stay app-local until a @flc/ui package extracts the shadcn primitives.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types.ts",
    "./platformRegistry": "./src/platformRegistry.ts",
    "./navUtils": "./src/navUtils.ts",
    "./routeChrome": "./src/routeChrome.ts"
  },
  "peerDependencies": {
    "react": "^18.0.0"
  }
}
